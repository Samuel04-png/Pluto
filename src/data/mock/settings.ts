import type { PlutoSettings } from "@/types";

export const demoSettings: PlutoSettings = {
  voiceEnabled: true,
  voiceStyle: "calm",
  voiceLanguage: "en-US",
  accentAdaptation: true,
  biometricEnabled: false,
  requireConfirmation: true,
  network: "devnet"
};
